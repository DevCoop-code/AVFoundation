# Playing a Video File Using AVPlayerLayer
* Configure a view to use an **AVPlayerLayer**
* Create an **AVPlayer**
* Create an **AVPlayerItem**(A file-based asset) and use **key-value observing** to observe its status
